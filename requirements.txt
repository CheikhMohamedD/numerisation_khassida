# --- Prétraitement image / PDF ---
pdf2image            # rasterisation (nécessite Poppler: brew install poppler)
opencv-python-headless
scikit-image
numpy
Pillow

# --- OCR de référence (M2) ---
pytesseract          # nécessite Tesseract + langue `ara` (brew install tesseract tesseract-lang)

# --- Reconstruction de police (M3) ---
fonttools            # construction TTF + features OpenType (feaLib)
uharfbuzz            # vérification du shaping arabe

# --- Segmentation ligne / OCR spécialisé ---
kraken               # HTR/OCR, RTL, historique (voie 1)
# calamari-ocr       # alternative CRNN+CTC
# transformers       # TrOCR / VLM (voie 2, via Soup)

# --- Entraînement modèles (via Soup CLI) ---
# Installer séparément selon la doc Soup :
#   pip install "soup-cli[train]"      # OCR-VLM + post-correction
#   pip install "soup-cli[vision]"     # modality: vision

# FontForge (optionnel, hors pip) : brew install fontforge — pour l'édition fine
# des glyphes vectorisés (le squelette v0 est construit avec fontTools ci-dessus).

# --- Évaluation ---
jiwer                # CER / WER
